Running make command will create an executable sf.
This is an example of using ScanFish Generic Classes.
This is not the ScanFish release. This is used only to
demonstrate using of classes and for further testing.
GUI is already ready, but we haven't joined it with logic yet.
But you can try sf to look at the first results.
This main.cpp sends frames every 3 secs and in parallel 
prints all messages in Net (from other nodes also).

You can try to change main.cpp.

